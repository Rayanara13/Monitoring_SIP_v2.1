# TODO — Monitoring SIP

---

## Этап 1: Переход на Socket.IO и автономность фронтенда
> Сделать интерфейс «живым» и убрать задержки.

- ✅ Скачивание `socket.io.min.js` и размещение в `static/`
- ✅ Замена CDN-ссылок в `panel.html` на локальные пути
- ✅ Установка Flask-SocketIO
- ✅ Инициализация `socketio` в `server.py`
- ✅ `broadcast_update` через `socketio.emit`
- ✅ Перевод `panel.html` с `setInterval` на события сокета
- ✅ Polling-фолбек при отключении сокета (с остановкой при переподключении)
- ✅ `refreshPending` — не пропускать события при параллельном fetch

---

## Этап 2: Оптимизация мониторинга (PING)

- ✅ Ускорение цикла: `time.sleep` сокращён до 10 секунд
- ✅ Мгновенное уведомление через сокет при изменении статуса
- ✅ Кросс-платформенный `ping_once` (Linux / Windows)
- ✅ Таймаут зависших состояний расширен на `Входящий_вызов` / `Исходящий_вызов`

---

## Этап 3: Безопасность

- ✅ Хэширование паролей (scrypt через werkzeug)
- ✅ Брутфорс-защита: блокировка IP после 7 попыток
- ✅ Персистентность блокировок (`users_data/login_blocks.json`)
- ✅ `event_token` изолирован по пользователям
- ✅ Rate-limit на `/event`: 120 запросов/мин с одного IP
- ✅ `SESSION_COOKIE_HTTPONLY`, `SameSite=Lax`
- ✅ `APP_SECRET_KEY` персистируется в `.env`
- ❌ ~~Привязка к железу (HWID) и файл лицензии~~ — избыточно для внутреннего инструмента
- ❌ ~~Обфускация через PyArmor~~ — избыточно, усложняет поддержку
- 🔲 HTTPS (self-signed сертификат) — актуально если сеть выходит за локалку

---

## Этап 4: Иерархия «Админ — Клиент»

- ✅ Дублирование GET-запросов на `TARGET_IP` (базовая синхронизация)
- ✅ `TARGET_IP` / `TARGET_PORT` вынесены в `.env`
- 🔲 Master Token для удалённого управления карточками через API
- 🔲 Сводка по филиалам в админ-панели (статусы всех подключённых серверов)

---

## Этап 5: Управление пользователями
> Управление через `auth.json` вручную — текущий рабочий способ. UI не приоритет.

- ❌ ~~Удаление пользователя через UI~~ — достаточно редактировать `auth.json`
- ❌ ~~Смена пароля через форму~~ — выдаётся при создании, меняется через `auth.json`
- ❌ ~~Перегенерация `event_token` через UI~~ — меняется в `auth.json` вручную
- ❌ ~~Роли «только просмотр»~~ — не актуально на текущем этапе тестирования

---

## Этап 6: Общая библиотека карточек (новое)

> Пользователь создаёт набор карточек и может пометить его как **«Общий»**.
> Другие пользователи видят общие наборы и могут импортировать их к себе одним кликом.

- ✅ Флаг `shared` в `users_data/meta_<username>.json`
- ✅ `GET /my_shared_status` — статус своего набора
- ✅ `GET /shared_sets` — список всех общих наборов с превью карточек
- ✅ `POST /toggle_shared` — переключить общий / приватный
- ✅ `POST /import_set` — импорт чужого набора (без перезаписи существующих номеров)
- ✅ Вкладка «Библиотека» в панели для всех пользователей
- ✅ Кнопка «Сделать общим / приватным» с подсказкой

---

## Этап 7: История и статистика

- 🔲 Лог звонков: записывать каждое событие (номер, собеседник, состояние, время) в SQLite
- 🔲 Вкладка «История» в панели — просмотр и фильтрация по номеру / дате
- 🔲 Экспорт истории в CSV
- 🔲 Статистика на дашборде: среднее время разговора, топ активных номеров

---

## Этап 8: UX / интерфейс

- 🔲 Поиск и фильтрация карточек по номеру или имени
- 🔲 Push-уведомления браузера (`Notification API`) при входящем звонке
- 🔲 Кастомный цвет / метка для карточки (например, отдел или этаж)
- 🔲 Компактный режим отображения (таблица вместо карточек)
- 🔲 Настройка таймаута зависших состояний через UI (сейчас хардкод 60 сек)
- ❌ ~~Звуковые уведомления~~ — не нужны

---

## Этап 9: Надёжность

- 🔲 Watchdog: автоперезапуск сервера при падении (systemd unit или supervisor)
- 🔲 Логирование в файл с ротацией (сейчас только `print` в консоль)
- 🔲 Health-check эндпоинт `/health` для мониторинга извне
- 🔲 Graceful shutdown: сохранение состояния при остановке сервера
